from .models import GameState


def update_enemies(state: GameState) -> None:
    """
    hàm xử lý lượt di chuyển của quái (mummy, scorpion).

    hiện tại để trống (stub). sau này sẽ:
    - duyệt qua từng enemy trong state.enemies
    - tính bước di chuyển theo luật của từng loại
    - cập nhật lại vị trí, check va chạm với player...
    """
    return
